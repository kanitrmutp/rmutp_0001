services:
  db: 
   image: postgres:latest
   container_name: my_db_compose
   ports:
    - "55433:5432"
   environment:
    - POSTGRES_DB=kanit
    - POSTGRES_USER=kanit
    - POSTGRES_PASSWORD=kanit

  app:
    image: prawee/strapi:latest
    container_name: my_app_compose
    ports:
      - "8080:1337"
    environment:
      - DATABASE_HOST=192.168.1.55
      - DATABASE_PORT=55432
      - DATABASE_NAME=kanit
      - DATABASE_USERNAME=kanit
      - DATABASE_PASSWORD=kanit
